export const metadata = {
  title: "Blog Details | User Panel",
  description: "Read article details and news updates.",
};

export default function Layout({ children }) {
  return <>{children}</>;
}
