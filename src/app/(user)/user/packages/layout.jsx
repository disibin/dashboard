export const metadata = {
  title: "Packages | User Panel",
  description: "View and subscribe to available service packages.",
};

export default function Layout({ children }) {
  return <>{children}</>;
}
