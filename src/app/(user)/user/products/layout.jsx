export const metadata = {
  title: "Products | User Panel",
  description: "Browse available products and software solutions.",
};

export default function Layout({ children }) {
  return <>{children}</>;
}
