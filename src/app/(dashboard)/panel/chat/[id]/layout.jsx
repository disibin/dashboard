export const metadata = {
  title: "Staff Chat Room | Disibin Dashboard",
  description: "Staff staff discussion and project collaboration chat.",
};

export default function Layout({ children }) {
  return <>{children}</>;
}
