export const metadata = {
  title: "Staff Login Logs | Disibin Dashboard",
  description: "Audit log of staff staff login sessions.",
};

export default function Layout({ children }) {
  return <>{children}</>;
}
