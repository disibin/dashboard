export const metadata = {
  title: "Add Staff Member | Disibin Dashboard",
  description: "Invite or create a new staff staff member account.",
};

export default function Layout({ children }) {
  return <>{children}</>;
}
