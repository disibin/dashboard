export const metadata = {
  title: "Tenants | Disibin Dashboard",
  description: "Manage multi-tenant organizations and accounts.",
};

export default function Layout({ children }) {
  return <>{children}</>;
}
