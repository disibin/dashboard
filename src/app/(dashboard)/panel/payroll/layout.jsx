export const metadata = {
  title: "Payroll | Disibin Dashboard",
  description: "Manage staff payroll, salary structures, and disbursements.",
};

export default function Layout({ children }) {
  return <>{children}</>;
}
