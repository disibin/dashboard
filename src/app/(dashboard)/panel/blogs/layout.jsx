export const metadata = {
  title: "Blogs | Disibin Dashboard",
  description: "Manage blogs, articles, and published content.",
};

export default function Layout({ children }) {
  return <>{children}</>;
}
