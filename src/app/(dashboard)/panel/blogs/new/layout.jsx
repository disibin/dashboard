export const metadata = {
  title: "Create New Blog | Disibin Dashboard",
  description: "Create and publish a new blog post.",
};

export default function Layout({ children }) {
  return <>{children}</>;
}
