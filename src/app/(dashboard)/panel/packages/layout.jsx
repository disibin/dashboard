export const metadata = {
  title: "Packages | Disibin Dashboard",
  description: "Manage pricing packages and service plans.",
};

export default function Layout({ children }) {
  return <>{children}</>;
}
